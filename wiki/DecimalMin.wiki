#sidebar sidebar

Returns the minimum value of a given field using decimal comparisons.  
The only passed value is the field that holds the minimum value you wish to return.  

This function is an alternative for the built-in MIN aggregation.  
It will change the return data type to DECIMAL.
