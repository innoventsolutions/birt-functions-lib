#sidebar sidebar

Returns the mode (the most common value) of a given field, using decimal comparisons.  
The only passed value is the field that holds the values you wish to use.
It will change the return data type to DECIMAL.

This function is an alternative for the built-in MODE aggregation.  
It will avoid minutae that sometimes occur in floating point calculations.
