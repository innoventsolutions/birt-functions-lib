#sidebar sidebar

Returns the first value of a given field.  
The only passed value is the field that holds the first value you wish to return.  

This function is an alternative for the built-in FIRST aggregation.  
The only difference is that it will change the return data type to DECIMAL.
