#sidebar sidebar

Returns the maximum value of a given field using decimal comparisons.  
The only passed value is the field that holds the maximum value you wish to return.  

This function is an alternative for the built-in MAX aggregation.  
It will change the return data type to DECIMAL.
