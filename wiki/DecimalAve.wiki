#sidebar sidebar

Returns the average of a given field, using decimal calculations.  
The only passed value is the field that holds the value you wish to average.  
It will change the return data type to DECIMAL.

This function is an alternative for the built-in AVE aggregation.  
It will avoid minutae that sometimes occur in floating point calculations.
