#sidebar sidebar

Returns the sum of a given field, using decimal calculations.
The only parameter is the field that holds the values you wish to sum.
It will change the return data type to DECIMAL.

This function is an alternative for the built-in SUM aggregation.  
It will avoid minutae that sometimes occur in floating point calculations.
