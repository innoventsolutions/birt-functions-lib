#sidebar sidebar

Returns the variance of a given field, using decimal calculations.
The only parameter is the field that holds the values you wish to use.
It will change the return data type to DECIMAL.

This function is an alternative for the built-in VARIANCE aggregation.  
It will avoid minutae that sometimes occur in floating point calculations.
